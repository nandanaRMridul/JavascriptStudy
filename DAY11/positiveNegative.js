

/* 
function checkPositiveOrNegative(num)
{
  (num > 0) ? console.log("Number is positive") : (num < 0) ? log("Number is negative") : console.log("Number is zero")
  
}

checkPositiveOrNegative(0);
 */

function checkPositiveOrNegative(num)
{
  return (num > 0) ? "Positive" : (num < 0) ? "Negative" : "Zero"
  
}

console.log(checkPositiveOrNegative(0)) ;